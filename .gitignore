node_modules

# Local overrides and secrets — never commit
.env
.env.local
.env.*.local

# Mode templates (.env.development, .env.staging, .env.production) are committed
!.env.example
!.env.development
!.env.staging
!.env.production

dist
